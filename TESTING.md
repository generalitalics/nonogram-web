# Инструкция по тестированию интеграции с бэкендом

## Проверка работы сохранения и загрузки прогресса

### 1. Откройте консоль браузера (F12 → Console)

Все запросы к бэкенду логируются с эмодзи:
- 🔐 - Логин
- 📤 - Отправка данных на бэкенд
- 📥 - Получение ответа от бэкенда
- ✅ - Успешная операция
- ❌ - Ошибка
- 💾 - Сохранение прогресса

### 2. Тестовые сценарии

#### Сценарий 1: Логин
1. Откройте http://localhost:5175
2. Введите username и password
3. В консоли должно появиться:
   - `🔐 Attempting login for user: <username>`
   - `📥 Login response status: 200 OK`
   - `✅ Login successful: {...}`

#### Сценарий 1.5: Загрузка прогресса при открытии уровня
1. После логина выберите сложность и уровень
2. В консоли должно появиться:
   - `📥 Loading progress from backend: {...}`
   - `📥 Load progress response status: 200 OK`
   - Если есть сохранённый прогресс:
     - `✅ Progress loaded: { has_progress: true, matrix_size: "[7x7]" }`
     - `✅ Loading saved progress into grid`
   - Если нет прогресса:
     - `ℹ️ No progress found for this level`
     - `ℹ️ No saved progress found, starting with empty grid`
3. Игровое поле должно заполниться сохранённой матрицей (если она есть)

#### Сценарий 2: Сохранение прогресса (кнопка Save)
1. Войдите в игру (выберите сложность → уровень)
2. Заполните несколько клеток
3. Нажмите кнопку "💾 Save"
4. В консоли должно появиться:
   - `💾 Manual save triggered`
   - `💾 Saving progress: reason="manual", level=1, difficulty="easy"`
   - `📤 Saving progress to backend: {...}`
   - `📥 Response status: 200 OK`
   - `✅ Progress saved successfully: {...}`

#### Сценарий 3: Автосохранение при выходе назад
1. Заполните несколько клеток
2. Нажмите "← Back to Levels"
3. В консоли должно появиться:
   - `💾 Saving progress: reason="back", level=1, difficulty="easy"`
   - `📤 Saving progress to backend: {...}`
   - `✅ Progress saved successfully: {...}`

#### Сценарий 4: Автосохранение при переходе на следующий уровень
1. Заполните несколько клеток
2. Нажмите "Next Level →"
3. В консоли должно появиться:
   - `💾 Saving progress: reason="next_level", level=1, difficulty="easy"`
   - `📤 Saving progress to backend: {...}`
   - `✅ Progress saved successfully: {...}`

#### Сценарий 5: Автосохранение при прохождении уровня
1. Заполните весь пазл правильно
2. При решении в консоли должно появиться:
   - `💾 Saving progress: reason="completed", level=1, difficulty="easy"`
   - `📤 Saving progress to backend: {...}`
   - `✅ Progress saved successfully: {...}`

### 3. Проверка в БД

После каждого сохранения проверьте таблицу `user_progress`:

```sql
SELECT 
    up.id,
    u.username,
    d.type as difficulty,
    l.number as level_number,
    up.status,
    up.reason,
    up.matrix,
    up.updated_at
FROM user_progress up
JOIN users u ON up.user_id = u.id
JOIN levels l ON up.level_id = l.id
JOIN difficulty d ON l.difficulty_id = d.id
ORDER BY up.updated_at DESC;
```

### 4. Формат данных, отправляемых на бэкенд

```json
{
  "username": "player1",
  "level": 1,
  "difficulty": "easy",
  "matrix": [[0,1,0], [1,1,1], ...],
  "reason": "manual"
}
```

### 5. Возможные ошибки

Если видите ошибки в консоли:
- `❌ Backend error:` - проверьте, что бэкенд запущен на порту 8000
- `⚠️ Cannot save progress: username is missing` - пользователь не залогинен
- `HTTP error! status: 500` - ошибка на бэкенде, проверьте логи бэкенда

### 6. Проверка прокси

Убедитесь, что прокси настроен правильно в `vite.config.js`:
- `/api/progress` → `http://localhost:8000/progress`
- `/api/auth` → `http://localhost:8000/auth`
- `/api/nonogram` → `http://localhost:8000/nonogram`

